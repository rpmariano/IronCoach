O trilho da prova — semanas feitas como pontos, marcador âmbar, fases por cima. No Início e no Hub.

```jsx
<RaceTrail weeks={18} current={6} startLabel="28 jul" endLabel="8 mar" />
```

- A única animação que vale repetir semanalmente: quando a semana muda, o marcador avança (1600ms).
